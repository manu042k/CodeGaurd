# Docker

This directory contains Docker configuration files for CodeGaurd.

## Files

- `Dockerfile` - Main application Dockerfile (to be created)
- `docker-compose.yml` - Docker Compose configuration (to be created)

## Usage

Instructions will be added when Docker configuration is implemented.
